# Use FREE-QUOTA accounts only. Comma-separated keys enable rotation.
GROQ_API_KEYS=replace_with_free_key_1,replace_with_free_key_2
OPENROUTER_API_KEYS=replace_with_free_key_1,replace_with_free_key_2
# For custom compatible endpoints, pass --api-key-env MY_PROVIDER_API_KEYS.
