# Frontend Notes

Follow the root `AGENTS.md` architecture rules. The frontend is responsible for presentation, routing, controls, typed API clients, and visualization.

Do not reimplement backend project logic in React/TypeScript. For project pages, call the shared FastAPI API and render the returned state.
